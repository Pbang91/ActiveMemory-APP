name: active_memory
description: "활쓴당 플젝임다"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  kakao_flutter_sdk_user: ^1.9.5
  flutter_dotenv: ^6.0.0
  flutter_riverpod: ^3.0.0
  dio: ^5.6.0
  retrofit: ^4.7.2
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.7.1
  retrofit_generator: ^10.0.5
  json_serializable: ^6.11.1
  intl: ^0.19.0

flutter:
  uses-material-design: true
  assets:
    - .env.dev
    - .env.prod
